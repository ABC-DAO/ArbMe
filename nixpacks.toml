[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build --workspace=@arbme/core-lib",
  "npm run build --workspace=@arbme/nextjs"
]

[start]
cmd = "cd bot && npm run server"
