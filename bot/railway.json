{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && pnpm install && pnpm --filter @arbme/core-lib build && pnpm --filter @arbme/nextjs build"
  },
  "deploy": {
    "startCommand": "cd bot && npm run server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
