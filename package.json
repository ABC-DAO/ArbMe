{
  "name": "arbme-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@arbme/nextjs",
    "build": "npm run build --workspaces --if-present",
    "bot": "npm run dev --workspace=@arbme/bot"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
